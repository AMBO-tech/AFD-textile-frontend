# AFD-Textile Frontend - Variables d'environnement
# Port par défaut du backend NestJS (Fastify) : 3000
VITE_API_URL=http://localhost:3000/api/v1
# Accès public du bucket Cloudflare R2 (domaine r2.dev ou personnalisé), sans barre finale.
# Défini : les photos sont lues directement chez Cloudflare. Vide : l'API les sert (bucket privé).
# VITE_MEDIA_URL=https://pub-xxxxxxxxxxxxxxxx.r2.dev
